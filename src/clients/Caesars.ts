import { IClientSettings } from '../interfaces'
import { WinePOS } from '../pos/types'

export const Caesars: IClientSettings = {
  name: 'Caesars',
  account: 1509,
  stores: [822],
  posType: WinePOS
}
